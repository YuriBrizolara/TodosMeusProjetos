const numero_lados = 6, soma_angulos = (180 * (numero_lados - 2));
let valor_angulo = (soma_angulos / numero_lados);
console.log(`soma dos ângulos internos S: ${soma_angulos}`);
console.log(`soma dos ângulos internos S: ${valor_angulo}`);
